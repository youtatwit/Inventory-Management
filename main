<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Top Slide-Out Toolbar</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>

  <div class="topbar" id="topbar">
    <div class="tool" title="Search">🔍</div>
    <div class="tool" title="Add">➕</div>
    <div class="tool" title="Settings">⚙️</div>
  </div>

  <div class="topbar-handle" id="handle">▼ Tools</div>

  <div class="content">
    <h3>Table View of Items</h3>
    <table>
      <tr><th>Name</th><th>Status</th></tr>
      <tr><td>Item 1</td><td>Active</td></tr>
      <tr><td>Item 2</td><td>Inactive</td></tr>
      <tr><td>Item 3</td><td>Active</td></tr>
    </table>
  </div>

  <script>
    const topbar = document.getElementById("topbar");
    const handle = document.getElementById("handle");

    function toggleBar() {
      const isOpen = topbar.classList.toggle("open");
      handle.textContent = isOpen ? "▲ Close" : "▼ Tools";
    }

    handle.addEventListener("click", toggleBar);

    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && topbar.classList.contains("open")) {
        toggleBar();
      }
    });
  </script>
</body>
</html>

